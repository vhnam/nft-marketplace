import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';

import Typography from './Typography';

<Meta title="Design System/Atoms/Typography" component={Typography} />

# Typography

This is the doc of the different Text / Typography components.

## Imports

```tsx
import { Typography } from 'design-system';
```

## Usage

<Canvas withSource="open">
  <Story
    name="Variants"
    decorators={[
      (Story) => (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
          <Story />
        </div>
      ),
    ]}
  >
    <Typography variant="hero">Hero</Typography>
    <Typography variant="headline1">Headline 1</Typography>
    <Typography variant="headline2">Headline 2</Typography>
    <Typography variant="headline3">Headline 3</Typography>
    <Typography variant="headline4">Headline 4</Typography>
    <Typography variant="body1">Body text large for big paragraphs.</Typography>
    <Typography variant="body1" fontWeight="bold">
      Body text large for big paragraphs.
    </Typography>
    <Typography variant="body2">
      Body text normal, used for most readble content.
    </Typography>
    <Typography variant="body2" fontWeight="bold">
      Body text bold, could be used for links and navigation elements.
    </Typography>
    <Typography variant="caption">
      Caption text, used for very small paragraphs.
    </Typography>
    <Typography variant="caption" fontWeight="bold">
      Caption text bold, could be used for secondary links or navigation
      elements.
    </Typography>
    <Typography variant="caption2">
      Caption text bold, could be used for secondary links or navigation
      elements.
    </Typography>
    <Typography variant="caption2" fontWeight="bold">
      Caption text bold, could be used for secondary links or navigation
      elements.
    </Typography>
    <Typography variant="hairline1">Hairline Large</Typography>
    <Typography variant="hairline2">Hairline Small</Typography>
    <Typography variant="button1">Button Default</Typography>
    <Typography variant="button2">Button Small</Typography>
  </Story>
</Canvas>

## Props

<ArgsTable of={Typography} />
